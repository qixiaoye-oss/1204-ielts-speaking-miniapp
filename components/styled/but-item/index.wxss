/**
 * but-item 组件样式
 *
 * 注意：主要样式已迁移至全局 button-group.wxss
 * 此组件通过外部样式类引用全局样式
 */

/* 图标旋转 - 组件特有 */
.rotate {
  transform: rotate(180deg);
}

/* 点击反馈效果 */
.tap-active {
  opacity: 0.7;
  transform: scale(0.98);
  transition: all 0.1s ease-out;
}
